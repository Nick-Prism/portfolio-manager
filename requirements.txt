# Zeta AI Portfolio Manager — Person 2 (Agent Engine) dependencies
# Install with: pip install -r requirements.txt

# LangGraph — agent orchestration
langgraph>=0.2.0
langchain-core>=0.2.0

# LLM providers (via LiteLLM)
litellm>=1.40.0

# Technical analysis
pandas-ta>=0.3.14b
pandas>=2.0.0
numpy>=1.26.0

# Data fetching (standalone — P3 replaces with their fetchers in prod)
yfinance>=0.2.40

# Web scraping for Screener.in (fundamental agent)
httpx>=0.27.0
beautifulsoup4>=4.12.0

# Scheduling
APScheduler>=3.10.0

# Data validation
pydantic>=2.0.0

# Async
anyio>=4.0.0

# Environment / config
python-dotenv>=1.0.0